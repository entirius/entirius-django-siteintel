# ERD configuration for django-siteintel — consumed by the ERD tooling (skill entirius-backend:erd-verify).
app_label: django_siteintel
groups:
  - name: siteintel-core
    title: "Audits, Reports & API Keys"
    models:
      - Audit
      - Report
      - ExternalApiKey
